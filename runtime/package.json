{
  "name": "runtime-now-plugin-dialpad",
  "version": "0.0.0",
  "private": true,
  "description": "Runtime functions for plugin-dialpad",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env --no-logs --live"
  },
  "dependencies": {
    "axios": "*",
    "morgan": "^1.9.1",
    "twilio-run": "cwkendall/twilio-run"
  }
}
